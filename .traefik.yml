displayName: Traefik API Key Auth
type: middleware
iconPath: .assets/icon.png

import: github.com/JimCronqvist/traefik-api-key-auth

summary: 'Protect your routes with API key(s).'

testData:
  headerName: X-Api-Key
  keys:
    - xyz
  bearerToken: false
  removeHeaderOnSuccess: true
